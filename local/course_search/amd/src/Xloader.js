/*global define: false */
define(['local_course_search/Xconfig', 'javascript/bootstrap-datepicker.min'], function () {
    // Return the third party module so we can depend on this loader and use the thirdparty module directly.

    // The responsiveslides plugin is a jQuery plugin that adds itself to the jQuery space.
    // No module export needed.
    // return {};
});
